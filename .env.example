# Google Cloud Agent Engine Configuration
# Get these values from your Agent Engine deployment on Google Cloud
AGENT_ENGINE_PROJECT_ID=your-gcp-project-id
AGENT_ENGINE_LOCATION=us-central1
AGENT_ENGINE_RESOURCE_ID=your-reasoning-engine-id

# Optional: Service Account for authentication
# If you want to use service account instead of gcloud CLI auth
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Server Configuration
PORT=3001

# Application Metadata
NEXT_PUBLIC_APP_NAME="ADK Annotation UI"
NEXT_PUBLIC_APP_DESCRIPTION="Direct Agent Engine Communication for Annotation Tasks"

# Optional: Development Settings
NEXT_PUBLIC_SHOW_DEV_CONSOLE=false
NEXT_PUBLIC_ENABLE_DEBUG=true

# Environment
NODE_ENV=development

# Note: Authentication
# By default, this project uses gcloud CLI for authentication
# Make sure to run: gcloud auth application-default login
# 
# For production, use a service account:
# 1. Create a service account in Google Cloud Console
# 2. Download the JSON key file
# 3. Set GOOGLE_APPLICATION_CREDENTIALS to the path of the JSON key
# 4. Update lib/agentEngineClient.ts to use the service account
