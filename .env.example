# Google Cloud Agent Engine Configuration
# Get these values from your Google Cloud Console

# Project Number (NOT Project ID)
# Find at: https://console.cloud.google.com/home/dashboard
# Note: Despite the variable name, this should be your PROJECT NUMBER (e.g., 255766800726)
AGENT_ENGINE_PROJECT_ID=your-gcp-project-number

# Region where your Agent Engine is deployed
AGENT_ENGINE_LOCATION=us-central1

# Reasoning Engine Resource ID
# Find in: Vertex AI > Reasoning Engines
AGENT_ENGINE_RESOURCE_ID=your-reasoning-engine-id

# Application Configuration
PORT=3001
NEXT_PUBLIC_APP_NAME="Feedback Workbench"

# Storage Configuration
# ==================================================================================
# USE_CLOUD_STORAGE: Enable Google Cloud Storage for persistent storage
#   - "true"  = Use GCS (required for Cloud Run, recommended for production)
#   - "false" = Use local filesystem (development/testing only)
#   Default: true
# ==================================================================================
USE_CLOUD_STORAGE=true

# Google Cloud Storage Configuration (required if USE_CLOUD_STORAGE=true)
# ==================================================================================

# GCS Bucket Name
# Create bucket: gsutil mb -l us-central1 gs://your-bucket-name
GCS_BUCKET_NAME=your-bucket-name

# Optional: Prefix for conversation files within the bucket
# Default: "conversations" (files will be stored as: conversations/conversation-id.json)
# Uncomment to customize:
# GCS_PREFIX=my-custom-prefix

# Authentication
# ==================================================================================
# For local development: gcloud auth application-default login
# For Docker: Use --mount-gcloud flag in docker-run.sh
# For Cloud Run: Automatic via service account
# Optional: Set custom credentials path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Environment
NODE_ENV=production
